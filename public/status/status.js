// status.js

// 目前这两个页面是纯静态的，不需要特定的JavaScript。
// 这个文件作为一个占位符存在，以便未来扩展。
// 例如，可以添加一个倒计时自动跳转回首页的功能。

document.addEventListener('DOMContentLoaded', () => {
  console.log('Status page loaded.');
  // 可以在这里添加未来的功能
});